import { BacklogTypePipe } from './backlog-type.pipe';

describe('BacklogTypePipe', () => {
  it('create an instance', () => {
    const pipe = new BacklogTypePipe();
    expect(pipe).toBeTruthy();
  });
});
